const number = (angka) => {
    for (let i = 0; i <= angka.length; i++) {
        let result = "";
        // if () {
        //   result += angka[i];
        // }
        console.log(result);
    }
};

// number([1, 2, 3, 4, 5]);

module.exports = number;
